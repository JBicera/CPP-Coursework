import 'package:flutter/material.dart';

class ThermodynamicsCalculatorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thermodynamics Calculator'),
      ),
      body: Center(
        child: Text('Thermodynamics Calculator Screen'),
      ),
    );
  }
}
